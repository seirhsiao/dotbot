# `init/`

```
create-dirs.sh            " creates essential directories
create-local-config.sh    " setup local configs
create-symlinks.sh        " setup symlinks so u can maintain ur dotfiles while you dotfile
helpers.sh                " colors n whatnot
init-git.sh               " init a git repository
restart.sh                " reboot ur machine
set-ssh.sh                " set, test, and add ssh keys
update-dotfiles.sh        " self explanatory
```
